<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ty.erp.daos.dao.CartoonDao">


    <insert id="Create">
        INSERT INTO Cartoon
        (
              RowId,
              Title,
              Des,
              CreateTime,
              UpdateTime,
              Creater,
              Pic,
              Author,
              VisitCount,
              FreeStatus,
              KeyWords,
              Description,
        )
        VALUES(
              #{RowId},
              #{Title},
              #{Des},
              #{CreateTime},
              #{UpdateTime},
              #{Creater},
              #{Pic},
              #{Author},
              #{VisitCount},
              #{FreeStatus},
              #{KeyWords},
              #{Description}
        )
    </insert>

    <insert id="Update">
        UPDATE Cartoon SET
        UpdateTime=now(),
        Describe=#{Describe},
        Title=#{Title},
        Pic=#{Pic},
        Author=#{Author},
        VisitCount=#{VisitCount},
        KeyWords=#{KeyWords},
        Description=#{Description}
        where RowId=#{Rowid}

    </insert>

    <select id="getList" resultType="Cartoon">
        SELECT * from Cartoon
    </select>

    <select id="getcount" resultType="long">
        SELECT COUNT(*) from Cartoon
    </select>

    <update id="changeEnable">
        UPDATE  Cartoon set Enable =(case when Enable=1 then 0 else 1 end) where rowid=#{RowId}
    </update>

    <select id="Get" resultType="Cartoon">
        SELECT  * FROM  Cartoon where RowId=#{RowId}
    </select>
</mapper>